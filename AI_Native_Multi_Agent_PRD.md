# SheetMetalAI - AI Native 多智能体协作升级方案 (PRD)

## 1. 背景与升级目标

当前 SheetMetalAI 项目处于“AI 赋能”阶段（单次大模型识别 + 填表交互）。为了彻底迈向 **真正的 AI Native 架构**，全面提升复杂图纸识别的准确率、容错率以及用户信任度，本方案决定引入 **智能体工作流 (Multi-Agent System)**。

升级的核心目标是：**将黑盒的 AI 识别过程转化为透明的、多角色协作的“虚拟钣金工程团队”工作流，并通过右侧的对话流向用户实时直播，最终实现人机对话式协同共创。**

---

## 2. 界面布局重构 (UI/UX)

在保持现有操作逻辑和核心功能不变的前提下，对界面空间进行重新划分，以容纳 AI 对话模块。

*   **左中区域（生产力工作区）：** 现有的“1. 上传图纸”、“参数设置”等功能面板向左侧压缩移动，3D 立体图 / 2D 展开图的渲染预览区占据屏幕中部的核心位置。
*   **右侧区域（AI 协同工作区）[新增]：** 在界面右侧开辟一个全新的 **Agent AI 对话交互模块 (Copilot 窗口)**。这不仅是一个聊天框，更是后台 AI 团队协作过程的“直播间”和用户介入决策的“指挥台”。

---

## 3. 核心场景与交互体验：多 Agent 工作流“直播”

### 3.1 触发机制：上传图纸 + 自然语言指令
系统在新增的 AI 交互场景中，不再是用户点击上传后“静默等待弹结果”的机器式体验。交互起点变为：
*   **用户动作：** 用户将图纸拖入聊天框，并可选择性输入先验指令（例如：“帮我看看这个件，好像是不锈钢的，展开要多大？”或直接回车）。或者用户直接输入文本指令（例如：“hi”、“把前面板的边长缩小5mm”）。
*   **系统响应：** 右侧的对话流将实时展现“虚拟工程团队”的协作过程。主控 Agent 接收到输入后，首先进行**意图识别 (Intent Recognition)**，将用户的意图分类并路由：
    1.  **`Greeting / Chat` (日常聊天)**：用户说 "hi"、"你能做什么"。AI 直接回复，不触发任何复杂操作。
    2.  **`AnalyzeDrawing` (分析图纸)**：用户上传了图片并说 "帮我分析这个"。触发图纸拆解、参数提取、质检的完整工作流。
    3.  **`ModifyParameter` (修改参数)**：用户说 "把前面板的边长缩小5mm"。AI 提取出目标（前面板）、属性（边长）、操作（缩小5mm），然后调用工具执行。
    4.  **`QueryData` (查询数据)**：用户说 "目前的板厚是多少？"。AI 从当前上下文中读取数据并回答。

### 3.2 协作场景“直播”呈现
*   **场景一：分析图纸**
    *   **主控 Agent (项目经理)** 冒泡：*"收到图纸和指令，注意该件可能为不锈钢。这是一张复杂的组合图，我已安排团队开始拆解。"*
    *   **Agent A (拆解员)** 冒泡：*"我已经把侧视图和主视图拆分完毕，已发给参数组。"*
    *   **Agent B (参数提取员)** 冒泡：*"正在处理主视图，提取尺寸中..."*
    *   **Agent C (质检员) 主动求助** 冒泡：*"等等，B 提取的板厚是 5mm，但我对比图纸上的比例尺，感觉更像是 2mm。老板（用户），这里图纸有点模糊，你确认一下是 2mm 还是 5mm？"*
*   **场景二：执行指令 (Tool Calling)**
    *   **用户**：*"把前面板的边长缩小5mm"*
    *   **主控 Agent (项目经理)** 冒泡：*"收到指令，正在为您将前面板的边长缩小5mm..."*
    *   **主控 Agent (项目经理)** 冒泡：*"✅ 修改成功！前面板的边长已从 100mm 更新为 95mm。"*

*   **场景三：日常聊天与数据查询**
    *   **用户**：*"hi"*
    *   **主控 Agent (项目经理)** 冒泡：*"你好！我是你的 SheetMetalAI 虚拟项目经理。你可以上传图纸让我帮你分析，或者直接告诉我你想修改哪个零件的参数。"*
    *   **用户**：*"目前的板厚是多少？"*
    *   **主控 Agent (项目经理)** 冒泡：*"当前提取到的板厚是 2mm。"*

**价值体现：**
1.  **先验知识注入：** 允许用户一开始就注入上下文，指导后续 Agent 的工作重点。
2.  **极高的信任感：** 用户清晰地知道 AI 在思考什么、做到了哪一步。
3.  **优雅的容错处理：** AI 把不确定性主动抛出，通过自然语言让用户低成本介入，避免了最后生成错误模型再让用户去表单里找错误。

---

## 4. 后端架构：多 Agent 角色、模型策略与 Tool Use

为后台的每个 Agent 赋予专属的职责、Prompt 设定和物理工具（API/函数），使它们成为真正具备执行力的智能体。

### 4.1 模型选择策略
*   **初期阶段（同模型扮演）：** 采用单一基座“多模态大模型”（如 Qwen-VL-Max、GPT-4o），通过不同 System Prompt 赋予不同角色面具，快速验证工作流。
*   **后期阶段（大小模型混搭降本增效）：**
    *   **视觉相关（拆解、提取）：** 必须使用最强多模态模型（Vision），处理 OCR 和空间几何理解。
    *   **逻辑校验（质检）：** 采用轻量级纯文本模型（或本地 7B 模型）搭配代码逻辑校验。
    *   **统筹规划（主控）：** 采用逻辑规划和 Tool Use 能力最强的文本/多模态模型。

### 4.2 Agent 角色定义与 Prompt 策划
每个 Agent 需要极度聚焦的 System Prompt (Persona + Task + Constraints + Format)。绝不能让“拆解员”去思考怎么“折弯”。

#### **主控 Agent (项目经理)**
*   **职责：** 接待用户，理解自然语言需求，进行意图识别（聊天、分析图纸、修改参数、查询数据等），调度工作流，调用工具执行指令，并向用户同步进度。
*   **赋予工具 (Tools)：** `Intent_Router` (意图路由)、`Assign_To_A` (派发任务)、`Update_Parameter` (修改参数工具)、`Sync_Status` (状态同步)。
*   **Prompt 示例：**
    *   **Role:** 你是 SheetMetalAI 的项目经理。你的职责是接待用户，理解用户的自然语言需求，判断用户的意图，并协调拆解员、提取员和质检员完成图纸展开工作，或者直接执行用户的参数修改指令。
    *   **Task:** 
        1. 当用户发送消息时，首先判断意图（Chat、AnalyzeDrawing、ModifyParameter、QueryData）。
        2. 如果是分析图纸，调用工具 `Assign_To_A` 开始工作流。
        3. 如果是修改参数，提取目标零件、属性和修改值，调用 `Update_Parameter` 工具执行修改。
        4. 你需要随时向用户同步进度和结果。
    *   **Constraint:** 你不能自己去看图提取参数，必须依赖你的下属。对用户要保持工程师般的专业和热情。对于修改指令，必须明确告知用户修改前后的值。

#### **Agent A (视图拆解员)**
*   **职责：** 统览全图，在后台智能裁切成独立的主视图、侧视图、俯视图等。
*   **赋予工具 (Tools)：** `Image_Crop` (图像自动裁剪与区域提取工具)。

#### **Agent B (参数提取员)**
*   **职责：** 从局部图中精准提取：厚度、材质、长宽、折弯段长度、角度等参数。
*   **赋予工具 (Tools)：** `Vision_Analyze` (大视觉分析提取)、`Query_Material_DB` (材质库查询)。
*   **Prompt 示例：**
    *   **Role:** 你是资深的钣金图纸分析员。
    *   **Task:** 你的唯一任务是从用户提供的局部图纸中，精准提取：厚度、材质、总长、总宽、各折弯段长度、角度。
    *   **Constraint:** 遇到看不清的数字，严禁瞎猜，必须在输出结果中将该字段标记为 `"confidence": "low"`。
    *   **Format:** 必须严格输出 JSON 格式的结构化数据，以便下游处理。

#### **Agent C (质检员)**
*   **职责：** 核对提取的数据与原图是否存在矛盾（如展开总长逻辑验证），发现矛盾则打回重做或向用户求助。
*   **赋予工具 (Tools)：** `Math_Calculate` (数学逻辑验算)、`Reject_To_B` (打回任务)。
*   **Prompt 示例：**
    *   **Role:** 你是严谨的钣金参数质检员。
    *   **Task:** 接收 Agent B 提取的数据。执行以下校验：1. 厚度是否在常规范围内 (0.5mm - 20mm)；2. 各个法兰边的长度之和，是否与标注的总长存在严重冲突。
    *   **Action:** 如果发现矛盾，或者收到 Agent B 标记为低置信度的数据，你要么调用工具 `Reject_To_B` 打回重做，要么输出一段话向用户求助确认。

#### **Agent D (工艺工程师) [新增]**
*   **职责：** 拿着确认无误的参数，负责成型与工艺推演。
*   **赋予工具 (Tools)：** `Generate_3D_Model` (渲染 3D 引擎模型)、`Simulate_Bending` (折弯工序模拟推演)。

---

## 5. 数据飞轮与自学习机制：数据库与 RAG (闭环反馈)

AI Native 架构必须具备记忆，系统不能只是“无状态的处理机”，而要“越干越聪明”。因此必须引入结构化数据库与 RAG 检索增强技术。

### 5.1 结构化数据存储 (MySQL)
系统需持久化记录业务数据和交互日志，作为商业化软件基础：
*   **关联存储：** 原始图纸、Agent B 首次提取的错误 JSON、用户的纠正指令（Chat History）、最终正确的 JSON。
*   **价值：** 保证系统状态的连贯性，支持用户查询历史记录。

### 5.2 RAG (检索增强生成) 知识库沉淀
最敏捷的模型进化方式是依靠 Vector DB (向量数据库) 构建 RAG 系统。

**前期没有账号体系怎么做？（基于“图纸特征”匹配）**
在未建立完善的账号体系（多租户）的 MVP 阶段，RAG 检索可以绑定“图纸特征”而非“用户账号”：
1. **提取特征：** 每次处理前，提取图纸右下角的“标题栏”内容（如公司名、图号前缀）或排版风格作为元数据（Metadata）。
2. **入库：** 存入向量数据库时带上元数据（例如 `{"drawing_style": "包含某某公司Logo"}`）。
3. **检索增强：** 下次匿名用户上传相似特征的图纸时，系统先基于图纸特征去 RAG 检索历史翻车记录（如：“处理带有某某公司字样的图纸时，厚度容易标在右上角”）。即便没有账号，只要是同一类图纸，也能实现经验复用和全局共享纠错。

**后续演进：** 随着商业化推进，中期可引入基于 LocalStorage 的匿名 UUID 记录个人偏好；远期再引入完整的企业多租户（Tenant-RAG）账号体系实现数据隔离。

1.  **沉淀知识 (写入)：** 当 Agent C 发现错误或用户介入纠正（如：“AI把厚度2看成5”），系统自动将这段经验转化为模式特征（如针对某类带有特定比例尺的图纸），存入 Vector DB (向量数据库)。
2.  **提取前检索 (查询)：** 当用户再次上传类似图纸时，系统在启动提取工作流前，先检索向量库获取历史教训。
3.  **动态注入 Prompt (增强)：** RAG 系统将检索到的历史教训，作为“注意事项 (Attention)”动态注入给 Agent B 的 System Prompt 中（例如：“提示：根据历史经验，此类图纸的厚度常标在右上角，请重点排查”）。

**总结：** 结构化数据库保证了交互记忆和业务状态，而 RAG 向量知识库则赋予了“虚拟工程团队”自我纠错和进化的能力，构成了真正的 AI Native 数据飞轮。