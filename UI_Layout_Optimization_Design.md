# SheetMetalAI - UI 界面布局优化设计文档

基于您的需求与截图反馈，我作为 UI 设计师对当前系统的界面布局进行了重构设计。当前布局的主要痛点是：
1. **中间 3D 预览区域过大**，导致两边信息被严重挤压。
2. **两边操作区（左侧表单、右侧对话）太小**，参数阅读困难，且限制了 AI 对话窗口的文本显示范围。
3. **顶部导航栏未满宽（未贴边）**，在宽屏下显得突兀，空间利用率低。

针对这些痛点，我设计了如下的布局优化方案：

## 1. 整体空间结构优化 (Global Layout Strategy)

我们将采用 **100vw x 100vh 的全屏弹性布局 (Flexbox)**。

### 1.1 顶部导航栏 (Header)
- **修改前**：使用了限制最大宽度的 `max-w-7xl mx-auto`，导致两边留白过多。
- **修改后**：取消最大宽度限制，改为 **`w-full px-6` (全宽 + 左右安全边距)**。
- **视觉效果**：Logo和右侧信息完全贴紧屏幕左右边缘，拉升整体应用的“专业工业软件”质感（类似 CAD 软件的无边框感）。

### 1.2 主体工作区 (Main Workspace)
我们将下方区域分为三个列（Left Sidebar, Center Viewer, Right Sidebar）。采用 `flex-1` 填满剩余高度，并重新分配水平比例。

#### 左侧：工具与参数面板 (Left Panel)
- **修改前**：宽度固定在 `300px`，过于狭窄，导致上传区域和参数调整框拥挤不堪。
- **修改后**：将宽度提升为 **`380px` - `420px`** （可以设为 `w-[400px]` 或 `min-w-[380px]`）。
- **优化点**：给参数控件、表单、上传按钮留出足够的呼吸感（Whitespace），让文字和输入框不再显得局促。

#### 中间：3D/2D 视图预览区 (Center Canvas)
- **修改前**：无限制膨胀（`flex-1`），占据了 70% 以上屏幕，导致大而无当。
- **修改后**：依然保持 `flex-1`，但因为左右两侧宽度的增加，中间区域自然会被压缩至 **50% - 55%** 的黄金比例。
- **优化点**：给画布增加合适的内边距（`p-6`），使模型与边界保持呼吸感。

#### 右侧：AI 智能对话面板 (Right Panel)
- **修改前**：宽度固定在 `350px`，在阅读长段落 AI 回复时非常容易折行。
- **修改后**：增加至 **`680px`** 或弹性占比 `w-[680px]`，将中间渲染部分再次缩减。
- **优化点**：大幅提升了 AI 对话窗口的文本显示范围，大语言模型（LLM）的输出通常包含代码、表格或详细分析，680px 能提供非常卓越的阅读与交互体验。

---

## 2. 详细样式与 CSS 修改指南 (Implementation Details)

为了达到上述效果，我们可以对 Tailwind CSS 的类名做以下调整：

### Header 区域
```tsx
// 旧代码
<header className="... max-w-7xl mx-auto flex justify-between items-center">

// 新代码
<header className="... w-full px-6 flex justify-between items-center">
```

### 侧边栏与中间区
```tsx
// 左侧栏调整：
<div className="w-[400px] min-w-[380px] border-r border-slate-800 flex flex-col ...">

// 右侧栏调整：
<div className="w-[680px] min-w-[680px] h-[calc(100vh-72px)] border-l border-slate-800 ...">

// 中间区（无需更改宽度逻辑，两侧增大后，中间自然缩小）：
<div className="flex-1 flex flex-col h-[calc(100vh-72px)]">
```

## 3. 视觉层级优化 (Visual Hierarchy)

- **边框与分割线**：使用 `border-slate-800` 将三大区域硬隔离，不再使用模糊的背景色差，增强功能区的划分。
- **间距 (Padding)**：模块内部统一使用 `p-4` (16px) 或 `p-6` (24px) 保证留白一致性。
- **排版对齐**：确保左侧上传按钮与参数面板的左侧处于同一垂直参考线，强迫症级别的对齐能极大提升软件的专业感。

---
**接下来我将主动为您修改代码文件（主要是 `App.tsx`），直接将上述设计应用到您的项目中！**